
create view [ASSET_ACCOUNTING].[STREET_ASSETS_EXPORT_VW] AS
select OBJECTID,
   STR_CODE,
   STR_NAME,
   STR_TYPE,
   GSA_NAME,
   FULL_NAME,
   STR_STATUS,
   OWN,
   CONVERT(varchar(20),CAST(DATE_ACCEPT as DATE)) DATE_ACCEPT,
   PST_CLASS,
   SOURCE,
   FDMID,
   FROM_STREET,
   TO_STREET,
   SHAPE_LENGTH,
   SHORT_DESC,
   LONG_DESC,
   SURF_MAT,
   SDI,
   PAVE_WIDTH,
   CONVERT(varchar(20),CAST(RATE_DATE as DATE)) RATE_DATE,
   NUM_CURB,
   WIDTH2,
   BASEVAL,
   SURFVAL,
   OLD_FDMID,
   CONVERT(varchar(20),CAST(DATE_RET as DATE)) DATE_RET,
   CONVERT(varchar(20),CAST(DATE_REV as DATE)) DATE_REV,
   CONVERT(varchar(20),CAST(DATE_ACT as DATE)) DATE_ACT,
   CONVERT(varchar(20),CAST(SYS_DATE as DATE)) SYS_DATE
from [ASSET_ACCOUNTING].[TRN_STREET_ASSETS]

